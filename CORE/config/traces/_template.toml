kind = "cmtrace_kind_here"
contains = ["TOKEN_IN_LOG_LINE"]   # e.g. ["CMORDER"]

id_key = "Id"
action_key = "Action"

timestamp_source = "prefix"
timestamp_format = "%H:%M:%S:%f"

capture_source_prefix = true